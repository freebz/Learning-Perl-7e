# The if Control Structure

if ($name gt 'fred') {
    print "'$name' comes after 'fred' in sorted order.\n";
}


if ($name gt 'fred') {
    print "'$name' comes after 'fred' in sorted order.\n";
} else {
    print "'$name' does not come after 'fred'.\n";
    print "Maybe it's the smae string, in fact.\n";
}
